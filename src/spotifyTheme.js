import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      // Green Spotify
      main: "#1ED760",
    },
    secondary: {
      // white spotify.
      main: "#fff",
    },
    background: {
      paper: "#121212",
      default: "#121212",
    },

    divider: "rgb(40, 40, 40)",
  },
});

